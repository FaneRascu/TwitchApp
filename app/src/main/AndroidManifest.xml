<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rascu.stefan.twitchapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activity.MainActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.activity.TopGamesActivity_"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>
        <activity
            android:name=".controller.activity.TopGamesActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>
        <activity
            android:name=".controller.activity.TopFeaturedStreamsActivity_"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>
        <activity
            android:name=".controller.activity.TopFeaturedStreamsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>

        <activity
         android:name=".controller.activity.TopStreamsActivity_"
         android:configChanges="orientation|screenSize"
         android:label="@string/app_name"
         android:theme="@style/AppTheme.NoActionBar">


         <!-- Parent activity meta-data to support 4.0 and lower -->
         <meta-data
         android:name="android.support.PARENT_ACTIVITY"
         android:value=".controller.activity.MainActivity" />
        </activity>
        <activity
            android:name=".controller.activity.TopStreamsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">


            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>

        <activity
            android:name=".controller.activity.TopCommunitiesActivity_"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>
        <activity
            android:name=".controller.activity.TopCommunitiesActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.activity.MainActivity" />
        </activity>
        <activity android:name=".controller.activity.CommunityActivity_" />
        <activity android:name=".controller.activity.CommunityActivity" />

        <activity android:name=".controller.activity.FeaturedStreamActivity_" />
        <activity android:name=".controller.activity.FeaturedStreamActivity" />

        <activity android:name=".controller.activity.GameActivity_" />
        <activity android:name=".controller.activity.GameActivity" />

        <activity android:name=".controller.activity.StreamActivity_"></activity>
        <activity android:name=".controller.activity.StreamActivity"></activity>
    </application>

</manifest>